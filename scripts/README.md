# Boilerplate Scripts

## Set up

### Make script executable
chmod +x scripts/spawn-custom-app.sh

### Spawn new custom app
./scripts/spawn-custom-app.sh schematic-guide-builder

### Follow the next steps shown in output
cd ../schematic-guide-builder
npm install
